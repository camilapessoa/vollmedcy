name: Cypress Tests

on:
  push:
    branches:
      - testes

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout
        uses: actions/checkout@v4
        with: 
          path: server

      - name: Start Server BackEnd
        run: |
          cd server
          npm install
          npm start &
          until [ "$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080)" == "200" ]; do
            echo "Waiting for server to start..."
            sleep 1
          done
          echo "Server is running successfully."
        working-directory: server
    
      - name: Configurar Variável de Ambiente
        run: echo "DB_ENV=${{ secrets.DB_ENV }}" > server/.env
        
      - name: Start Web App FrontEnd
        run: |
          cd web
          npm install
          npm start &
      # - name : Criar variável de ambiente Cypress
      #   run: echo "CYPRESS_ENV_CI={{ secrets.CYPRESS_ENV_CI }}" > /cypress.env.json

      - name: Run Cypress Tests
        uses: cypress-io/github-action@v6
        with:
              # cypress/e2e/cadastro-sucesso.cy.js
              # cypress/e2e/login-clinica.cy.js
              # cypress/e2e/dashboard.cy.js

          wait-on: 'http://localhost:3000 , http://localhost:8080'
          record: true
          parallel: true # Runs test in parallel using settings above 
          group: 'Cypress Tests'
        env:
        # browser: chrome
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# name: Cypress Tests

# on:
#   push:
#     branches:
#       - testes

# jobs:
#   cypress-run:
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18.19.0'
#       - name: Show DB_ENV
#         run: |
#            echo 'DB_ENV: ${{ secrets.DB_ENV }}' > .env
#       - name: Install Dependencies
#         run: npm install
#       - name: Start Server BackEnd
#         run: |
#           cd server
#           npm install 
#           nohup npm start &
      
#       - name: Start Web App FrontEnd
#         run: |
#           cd web
#           npm install
#           nohup npm start &

#       - name: Write the cypress.env.json file
#           # use quotes around the secret, as its value
#           # is simply inserted as a string into the command
#         run: |
#           echo '${{ secrets.CYPRESS_ENV_CI }}' > cypress.env.json

#       - name: Run Cypress Tests
#         uses: cypress-io/github-action@v6

#         with:
#           wait-on: 'http://localhost:8080/clinica'
#           # wait-on-timeout: 180
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

              
